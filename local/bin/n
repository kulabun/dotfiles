#!/bin/bash

FILE_EXTENSION=".note"
NOTES_DIR="$HOME/notes"

if [ "$#" -gt 0  ]; then 
  note_file=$(ls "$NOTES_DIR/" | fzf -q "$@" | sed 's!.*/!!')
else
  note_file=$(ls "$NOTES_DIR/" | fzf | sed 's!.*/!!')
fi

[ "$note_file" != "" ] && $EDITOR "$NOTES_DIR/$note_file"
